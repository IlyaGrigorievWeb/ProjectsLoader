namespace ProjectsLoader.Services;

public class GitHubService
{
    // get by id
    // get all [with filter, for now just by framework]
    // get meta info by url without loading
    // save by url (download and save meta info in database)
    // save by entity (just save metainfo without loading)
    // update by url (sent request, if something changed on github -> update it in database)
    // delete if loaded from disk space, not from database
}